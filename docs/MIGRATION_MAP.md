# Migración desde V6.x

`visualWorld`: `cosmos|aurora|solar → storm`, `lunar → ocean`, `ocean → ocean`, `forest → forest`.

La clave `theme` conserva compatibilidad: `dark` = oscuro; `normal` = claro. No se cambia la clave de datos `mi-campo.data.v1`.

La edición MASTER añade archivos, pero no requiere convertir sesiones anteriores.
